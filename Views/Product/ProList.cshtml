<link href="~/css/dashboard.css" rel="stylesheet">
<link href="~/css/dataTables.bootstrap.css" rel="stylesheet">
<link href="~/css/index.css" rel="stylesheet">


                <div class="table-responsive" style="margin-top: 3px; margin-left: 0px; margin-right: 10px">
                    <div class="panel panel-primary">
                        <div class="panel-heading">產品清單列表</div>
                        <div class="panel-body">
                            <table id="myDataTable" class="table table-striped table-bordered table-condensed" style="">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>產品編號</th>
                                        <th width="45%">產品名稱</th>
                                        <th>價格</th>
                                        <th>特價</th>
                                        <th>分類</th>
                                        <th>啟用</th>
                                        <th colspan="2" width="15%"></th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                        </div>
                    </div>
                </div>


<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title" id="myModalLabel">刪除確認</h4>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    <label for="inputProClassNo">產品編號：</label>
                    <label id="label-ProNo"></label>
                </div>
                <div class="form-group">
                    <label for="inputProClassNo">產品名稱：</label>
                    <label id="label-ProName"></label>
                </div>
                <div class="form-group">
                    <form id="myDeleteForm" action="@Url.Action("ProDelete")" method="POST">
                        <input type="hidden" class="form-control col-lg-2" id="inputAppSer" name="appSer" value="">
                    </form>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">取消</button>
                <button type="button" class="btn btn-primary" id="btn-delete-submit" onclick="deleteFromSubmit()">刪除</button>
            </div>
        </div>
    </div>
</div>

        <!-- Bootstrap core JavaScript
        ================================================== -->
        <!-- Placed at the end of the document so the pages load faster -->

        <script src="../../js/jquery.dataTables.js"></script>
        <script src="../../js/dataTables.bootstrap.js"></script>
        <script src="../../js/app/pro_list.js"></script>
        <script src="../../js/jquery.isloading.min.js"></script>

